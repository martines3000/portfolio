/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: 'https://portfolio.skippy-ai.com',
  generateRobotsTxt: true,
  generateIndexSitemap: false,
  sitemapSize: 1000,
};

module.exports = config;
